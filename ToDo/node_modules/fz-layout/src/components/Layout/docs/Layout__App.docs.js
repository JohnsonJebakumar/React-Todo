import React from 'react';
import PropTypes from 'prop-types';
import Container from '../Container';
import Box from '../Box';

export default class Layout__App extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <div style={{ height: '95vh' }}>
        <Container alignBox="column" style={{ background: '#f5f5f5' }}>
          <Box eleRef={ref => console.log('getting dom', ref)}>
            <div style={{ background: '#666', height: '45px' }} />
          </Box>
          <Box flexible />
        </Container>
      </div>
    );
  }
}

Layout__App.docs = {
  componentGroup: 'Template'
};
