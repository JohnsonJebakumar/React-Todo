import React, { Component } from 'react';
import PropTypes from 'prop-types';
import Grid from '../Grid';
import Col from '../Col';
import Row from '../Row';
function Box() {
  return <div style={{ background: '#007fff', minHeight: '1rem' }}>asdas</div>;
}
export default class Layout__RowAutoHeight extends Component {
  constructor(props) {
    super(props);
  }
  render() {
    return (
      <Grid style={{ width: '100%' }}>
        <Row style={{ height: '100vh', flexDirection: 'column' }}>
          <Col
            style={{
              background: 'blue'
            }}
          >
            Header
          </Col>
          <Col
            xs
            sm
            md
            lg
            style={{
              background: 'gray'
            }}
          />
        </Row>
      </Grid>
    );
  }
}
if (__DOCS__) {
  Layout__RowAutoHeight.docs = {
    componentGroup: 'Template'
  };
}
